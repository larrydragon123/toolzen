# ToolZen i18n Chinese & English Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add bilingual (zh/en) support to ToolZen with path-prefixed URLs, automatic language detection, and a language switcher.

**Architecture:** Centralized translation dictionaries under `src/i18n/` with type-safe `createTranslator(lang)`. Pages move to `src/pages/[lang]/` using Astro's `getStaticPaths()` to generate both language variants from a single source file. Preact islands use a `useLanguage()` hook reading `document.documentElement.lang`.

**Tech Stack:** Astro 6, Preact, TypeScript, Tailwind CSS

---

## Task 1: Create i18n type system and translator utility

**Files:**
- Create: `src/i18n/ui.ts`
- Create: `src/i18n/index.ts`

- [ ] **Step 1: Write `src/i18n/ui.ts` — TranslationDict interface and createTranslator factory**

```typescript
// src/i18n/ui.ts

export interface HomeDict {
  hero: string;
  heroHighlight: string;
  subtitle: string;
  popular: string;
  allCategories: string;
  whyTitle: string;
  privacy: { title: string; desc: string };
  speed: { title: string; desc: string };
  free: { title: string; desc: string };
  faqTitle: string;
  faq: Array<{ q: string; a: string }>;
}

export interface NavDict {
  dev: string;
  text: string;
  image: string;
  crypto: string;
  calculators: string;
  searchPlaceholder: string;
  searchHint: string;
  searchEmpty: string;
}

export interface FooterDict {
  copyright: string;
  about: string;
  privacy: string;
  contact: string;
}

export interface CategoryMeta {
  title: string;
  description: string;
}

export interface ToolMeta {
  title: string;
  description: string;
  keywords: string[];
  howToTitle: string;
  howTo: string;
  faq: Array<{ q: string; a: string }>;
}

export interface SeoDict {
  homeTitle: string;
  homeDesc: string;
  aboutTitle: string;
  aboutDesc: string;
  privacyTitle: string;
  privacyDesc: string;
  categoryPrefix: string;
}

export interface UIDict {
  format: string; compress: string; validate: string; copy: string;
  encode: string; decode: string; swap: string; generate: string; download: string;
  calculate: string; clear: string;
  characters: string; charactersNoSpaces: string; words: string; lines: string; bytes: string;
  input: string; output: string; typeOrPaste: string; outputPlaceholder: string;
  content: string; size: string; fgColor: string; bgColor: string;
  enterContent: string; downloadPng: string; generateFailed: string;
  loanAmount: string; annualRate: string; loanYears: string;
  repaymentMethod: string; equalPayment: string; equalPrincipal: string;
  monthlyPayment: string; totalPayment: string; totalInterest: string;
  totalMonths: string; viewSchedule: string; period: string;
  principal: string; interest: string; balance: string;
  height: string; weight: string; yourBmi: string;
  underweight: string; normal: string; overweight: string; obese: string;
  dateDiff: string; dateAdd: string; startDate: string; endDate: string;
  addDays: string; totalDays: string; naturalDays: string;
  workdays: string; weekdays: string; weekends: string; targetDate: string;
  calcDiff: string; calcDate: string; weekDayNames: string[];
  upperCase: string; lowerCase: string; titleCase: string;
  s2t: string; t2s: string; numS2F: string; numF2S: string; invertCase: string;
  enterText: string; result: string;
  selectAlgorithm: string; generateUuid: string; copyAll: string;
  selectImage: string; quality: string; compressImage: string;
  pattern: string; testString: string;
  originalText: string; modifiedText: string;
  pickColor: string;
  sourceTimezone: string; targetTimezone: string; convertTime: string;
}

export interface TranslationDict {
  site: { name: string; tagline: string };
  home: HomeDict;
  nav: NavDict;
  footer: FooterDict;
  categories: Record<string, CategoryMeta>;
  tools: Record<string, ToolMeta>;
  about: { h1: string; p1: string; p2: string; p3: string; contactTitle: string; contact: string };
  privacy: { h1: string; updated: string; noCollectTitle: string; noCollect: string; analyticsTitle: string; analytics: string; cookiesTitle: string; cookies: string; contactTitle: string; contact: string };
  seo: SeoDict;
  ui: UIDict;
  relatedTools: string;
  toolCount: (n: number) => string;
  breadcrumbHome: string;
}

export type Lang = 'zh' | 'en';
export type Translations = TranslationDict;

export function createTranslator(dict: TranslationDict) {
  return dict;
}
```

- [ ] **Step 2: Write `src/i18n/index.ts` — re-exports and helpers**

```typescript
// src/i18n/index.ts
import type { Lang, Translations } from './ui';
import { zh } from './zh';
import { en } from './en';

export type { Lang, Translations } from './ui';
export { createTranslator, type TranslationDict, type ToolMeta, type CategoryMeta } from './ui';

const DICTS: Record<Lang, Translations> = { zh, en };

export function getDict(lang: Lang): Translations {
  return DICTS[lang];
}

export function getLangFromPath(pathname: string): Lang {
  const seg = pathname.split('/')[1];
  if (seg === 'en') return 'en';
  return 'zh';
}

export function getOtherLang(lang: Lang): Lang {
  return lang === 'zh' ? 'en' : 'zh';
}

export function switchPath(pathname: string): string {
  const parts = pathname.split('/').filter(Boolean);
  if (parts[0] === 'zh' || parts[0] === 'en') {
    parts[0] = parts[0] === 'zh' ? 'en' : 'zh';
  }
  return '/' + parts.join('/') + '/';
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx astro check
```

- [ ] **Step 4: Commit**

```bash
git add src/i18n/ui.ts src/i18n/index.ts
git commit -m "feat: add i18n type system and translation helpers"
```

---

## Task 2: Create Chinese dictionary (zh.ts)

**Files:**
- Create: `src/i18n/zh.ts`

- [ ] **Step 1: Write `src/i18n/zh.ts` — all Chinese text extracted from existing pages**

```typescript
// src/i18n/zh.ts
import type { TranslationDict } from './ui';

export const zh: TranslationDict = {
  site: {
    name: 'ToolZen',
    tagline: '简即是多',
  },
  home: {
    hero: '工具之道，',
    heroHighlight: '简即是多',
    subtitle: '15+ 免费在线工具。隐私优先，无需上传。所有处理在浏览器端完成。',
    popular: '⭐ 热门工具',
    allCategories: '📂 全部分类',
    whyTitle: '为什么选择 ToolZen？',
    privacy: { title: '隐私优先', desc: '所有工具在您的浏览器本地运行，数据绝不会上传到任何服务器。' },
    speed: { title: '极速响应', desc: '纯静态页面，全球CDN加速，输入即输出，无需等待。' },
    free: { title: '完全免费', desc: '所有工具永久免费使用，无隐藏收费，无功能限制。' },
    faqTitle: '常见问题',
    faq: [
      { q: 'ToolZen的工具真的免费吗？', a: '是的，所有工具永久免费。我们的收入来自广告（未来），不会向用户收费。' },
      { q: '我的数据安全吗？', a: '绝对安全。所有数据处理都在您的浏览器中完成，数据不会离开您的设备。' },
      { q: '需要注册账号吗？', a: '不需要。所有工具无需注册即可直接使用。' },
    ],
  },
  nav: {
    dev: '开发者工具',
    text: '文本工具',
    image: '图片工具',
    crypto: '编码加密',
    calculators: '计算器',
    searchPlaceholder: '搜索工具... (如 JSON、二维码、Base64)',
    searchHint: '输入关键词搜索工具',
    searchEmpty: '未找到匹配的工具',
  },
  footer: {
    copyright: '© 2026 ToolZen. 所有工具均在您的浏览器本地运行。',
    about: '关于',
    privacy: '隐私',
    contact: '联系',
  },
  categories: {
    dev: { title: '开发者工具', description: 'JSON、Base64、正则等开发者常用工具' },
    text: { title: '文本工具', description: '文本对比、字数统计等文字处理工具' },
    image: { title: '图片工具', description: '图片压缩、二维码生成等图像工具' },
    crypto: { title: '编码加密', description: 'MD5、SHA哈希、URL编解码工具' },
    calculators: { title: '计算器', description: '房贷、BMI等实用计算器' },
  },
  tools: {
    'json-formatter': {
      title: 'JSON 格式化',
      description: '在线JSON格式化、验证和压缩工具。支持语法高亮和错误定位，所有处理在浏览器端完成。',
      keywords: ['JSON格式化', 'JSON验证', '在线JSON工具', 'JSON美化', 'JSON压缩'],
      howToTitle: '如何使用JSON格式化工具',
      howTo: '将需要处理的JSON数据粘贴到输入框中，点击"Format"按钮即可自动美化排版。支持压缩JSON为单行格式、验证JSON语法正确性，以及一键复制结果。所有处理均在浏览器本地完成，您的数据不会上传到任何服务器，安全可靠。',
      faq: [
        { q: 'JSON格式化有什么用？', a: 'JSON格式化可以将压缩的JSON数据美化为带缩进的易读格式，方便开发者阅读和调试。无论是API返回的数据还是配置文件，格式化后都能快速理解数据结构。' },
        { q: '数据会上传到服务器吗？', a: '不会。ToolZen所有工具都在浏览器本地运行，您的数据始终保留在您的设备上。我们不会收集、存储或传输您输入的任何内容。' },
        { q: '支持多大的JSON文件？', a: 'JSON格式化工具在浏览器中运行，理论上可处理数MB的JSON数据。但对于超大文件，建议先进行拆分处理以确保浏览器性能。验证功能可帮助您在格式化前确认JSON语法是否正确。' },
      ],
    },
    'base64': {
      title: 'Base64 编解码',
      description: '在线Base64编码解码工具。支持文本和文件的相互转换，数据不会上传到服务器。',
      keywords: ['Base64编码', 'Base64解码', '在线Base64', '图片转Base64', 'Base64转换'],
      howToTitle: '如何使用Base64编解码工具',
      howTo: '在左侧输入框中粘贴要编码或解码的文本，点击"Encode"进行Base64编码，或点击"Decode"进行解码。支持中文等Unicode字符的正确编解码，处理结果一目了然。所有运算均在您的浏览器中完成，数据绝不会离开您的设备。',
      faq: [
        { q: '什么是Base64编码？', a: 'Base64是一种将二进制数据转换为ASCII字符串的编码方式，常用于在文本协议中传输图片、文件等二进制内容。它将每3个字节编码为4个可打印字符，广泛应用于邮件附件、Data URI和API数据传输。' },
        { q: '数据会上传到服务器吗？', a: '不会。ToolZen所有工具都在浏览器本地运行，您的数据始终保留在您的设备上。' },
        { q: 'Base64编码会增加数据多大？', a: 'Base64编码会使数据体积增加约33%。这是因为每3个字节的原始数据被编码为4个字节。如果您的应用对数据传输量有严格要求，建议在必要时再使用Base64编码。' },
      ],
    },
    'uuid-generator': {
      title: 'UUID 生成器',
      description: '在线生成UUID v4和ULID标识符。支持批量生成和一键复制，完全在浏览器端运行。',
      keywords: ['UUID生成', '在线UUID', 'GUID生成器', 'ULID生成', '唯一标识符'],
      howToTitle: '如何使用UUID生成器',
      howTo: '页面加载后自动生成UUID v4标识符。点击"生成"按钮可批量生成多个UUID，支持一键复制单个或全部结果。所有生成过程在浏览器端完成，无需网络请求。',
      faq: [
        { q: 'UUID v4是什么？', a: 'UUID v4是基于随机数生成的通用唯一标识符，碰撞概率极低，广泛用于数据库主键、分布式系统等场景。' },
        { q: '生成的UUID是唯一的吗？', a: 'UUID v4使用随机数生成，重复概率约为1/2^122，实际上可以认为是唯一的。' },
      ],
    },
    'qr-code': {
      title: '二维码生成器',
      description: '免费在线二维码生成工具。支持URL、文本、联系方式等多种类型，可自定义颜色和大小。',
      keywords: ['二维码生成', 'QR码在线', '免费二维码', '在线生成二维码', 'QR Code'],
      howToTitle: '如何使用二维码生成器',
      howTo: '输入URL或文本内容，调整尺寸和颜色参数，点击"生成"按钮即可创建二维码。支持自定义前景色和背景色，可下载为PNG图片。',
      faq: [
        { q: '生成的二维码会过期吗？', a: '不会。二维码只是将文本信息编码为图形格式，只要编码的内容有效，二维码就可以永久使用。' },
        { q: '二维码支持中文吗？', a: '支持。输入中文内容后会自动选择适合的编码方式生成二维码。' },
      ],
    },
    'image-compress': {
      title: '图片压缩',
      description: '在线图片压缩工具，支持PNG、JPEG、WebP格式。压缩过程在浏览器本地完成，保护隐私。',
      keywords: ['图片压缩', '在线压缩图片', 'PNG压缩', 'JPEG压缩', '免费图片压缩'],
      howToTitle: '如何使用图片压缩工具',
      howTo: '拖拽或选择图片文件到上传区域，调整压缩质量参数，即可实时预览压缩效果并下载压缩后的图片。所有压缩操作在浏览器本地完成，图片不会上传到服务器。',
      faq: [
        { q: '图片压缩会影响质量吗？', a: '适当压缩基本不影响肉眼观察，您可以在下载前预览压缩效果，根据需求调整质量参数。' },
        { q: '支持哪些图片格式？', a: '支持PNG、JPEG和WebP格式的图片压缩。' },
      ],
    },
    'text-diff': {
      title: '文本对比',
      description: '在线文本差异对比工具。快速找出两段文本的增删改内容，支持并排和统一视图。',
      keywords: ['文本对比', '文本差异', 'Diff工具', '在线Diff', '代码对比'],
      howToTitle: '如何使用文本对比工具',
      howTo: '在左右两个文本框中分别粘贴原始文本和修改后的文本，工具会自动高亮显示差异内容。绿色表示新增，红色表示删除。',
      faq: [
        { q: '文本对比支持哪些格式？', a: '支持纯文本对比，适用于代码、文档、配置文件等任意文本格式。' },
        { q: '对比结果准确吗？', a: '使用标准的diff算法逐行对比，能够准确识别增删改内容。' },
      ],
    },
    'word-counter': {
      title: '字数统计',
      description: '在线字数统计工具。实时统计字符数、单词数、行数和段落数，支持中英文混合统计。',
      keywords: ['字数统计', '在线字数', '字符计数', '单词计数', '文本统计'],
      howToTitle: '如何使用字数统计工具',
      howTo: '在文本框中输入或粘贴文本，工具会实时显示字符数、单词数、行数和字节数等统计信息。',
      faq: [
        { q: '中文如何统计字数？', a: '中文按字符数统计，每个汉字计为一个字符。单词数统计时中文字符不计入英文单词计数。' },
        { q: '数据安全吗？', a: '所有统计在浏览器本地完成，文本内容不会上传到服务器。' },
      ],
    },
    'regex-tester': {
      title: '正则表达式测试器',
      description: '在线正则表达式测试工具。实时匹配高亮，支持常用正则模式库，所有计算在浏览器完成。',
      keywords: ['正则测试', '正则表达式', '在线正则', 'Regex测试', '正则匹配'],
      howToTitle: '如何使用正则表达式测试器',
      howTo: '输入正则表达式和测试文本，工具会实时高亮所有匹配结果。支持全局匹配、分组捕获等常用功能。',
      faq: [
        { q: '支持哪些正则语法？', a: '支持JavaScript标准正则表达式语法，包括分组、量词、断言等。' },
        { q: '正则表达式如何处理中文？', a: '支持Unicode属性转义，可以匹配中文字符范围。' },
      ],
    },
    'color-picker': {
      title: '颜色选择器',
      description: '在线颜色选择和转换工具。支持HEX、RGB、HSL格式互转，提供调色板和取色功能。',
      keywords: ['颜色选择器', '取色器', 'HEX转RGB', '在线配色', 'Color Picker'],
      howToTitle: '如何使用颜色选择器',
      howTo: '使用取色器选择颜色，或直接输入HEX/RGB/HSL值。工具会自动转换并显示各种格式的颜色值。',
      faq: [
        { q: '支持哪些颜色格式？', a: '支持HEX、RGB、HSL三种格式的相互转换。' },
        { q: '可以保存颜色方案吗？', a: '可以使用浏览器的取色器功能从屏幕上任意位置取色。' },
      ],
    },
    'md5-hash': {
      title: 'MD5 / SHA 哈希生成',
      description: '在线MD5、SHA-1、SHA-256哈希值生成工具。支持文本和文件哈希，所有计算在浏览器端完成。',
      keywords: ['MD5生成', 'SHA256', '哈希生成', '在线MD5', '文件哈希'],
      howToTitle: '如何使用哈希生成工具',
      howTo: '输入文本或选择文件，选择哈希算法（MD5、SHA-1、SHA-256），即可生成对应的哈希值。所有计算在浏览器端完成。',
      faq: [
        { q: 'MD5安全吗？', a: 'MD5已被证明存在碰撞安全问题是不可靠的，不应用于安全敏感场景。对于安全需求请使用SHA-256。' },
        { q: '文件哈希如何计算？', a: '选择文件后，工具会在浏览器中读取文件内容并计算哈希值，文件不会上传到服务器。' },
      ],
    },
    'url-encode': {
      title: 'URL 编解码',
      description: '在线URL编码和解码工具。快速对URL特殊字符进行encodeURI和decodeURI转换。',
      keywords: ['URL编码', 'URL解码', 'encodeURI', 'decodeURI', 'URL转义'],
      howToTitle: '如何使用URL编解码工具',
      howTo: '输入要编码或解码的文本，点击相应按钮即可进行URL编码或解码操作。',
      faq: [
        { q: 'URL编码和解码的区别？', a: 'URL编码将特殊字符转换为%XX格式以便在URL中传输，解码则是反向操作将%XX还原为原始字符。' },
        { q: '数据会上传吗？', a: '不会。所有转换在浏览器本地完成。' },
      ],
    },
    'mortgage-calculator': {
      title: '房贷计算器',
      description: '在线房贷计算器。支持等额本息和等额本金两种还款方式，输入贷款金额、年利率和期限即可计算月供和总利息。',
      keywords: ['房贷计算器', '月供计算', '等额本息', '等额本金', '贷款利率计算'],
      howToTitle: '如何使用房贷计算器',
      howTo: '输入贷款金额、年利率和贷款年限，选择还款方式（等额本息或等额本金），点击计算即可查看月供、利息总额和详细还款计划表。',
      faq: [
        { q: '等额本息和等额本金的区别？', a: '等额本息每月还款额固定，前期利息占比高；等额本金每月还款额递减，总利息更少但前期压力较大。' },
        { q: '计算结果准确吗？', a: '计算结果基于标准公式，仅供参考。实际房贷可能涉及其他费用，请以银行最终计算为准。' },
      ],
    },
    'bmi-calculator': {
      title: 'BMI 计算器',
      description: '在线BMI身体质量指数计算器。输入身高和体重，自动计算BMI值和健康状态评估。',
      keywords: ['BMI计算器', '身体质量指数', '体重指数', '在线BMI', '健康体重'],
      howToTitle: '如何使用BMI计算器',
      howTo: '输入身高和体重，工具会自动计算BMI值并根据世界卫生组织标准显示体重状态（偏瘦、正常、偏胖、肥胖）。',
      faq: [
        { q: 'BMI计算准确吗？', a: 'BMI是一个参考指标，不适用于运动员、孕妇等特殊人群。如有健康疑问请咨询专业医生。' },
        { q: 'BMI正常范围是多少？', a: '根据世界卫生组织标准，BMI在18.5-24之间为正常范围。' },
      ],
    },
    'date-calculator': {
      title: '日期计算器',
      description: '在线日期计算器。可视化日历选择，计算两个日期之间的天数、工作日数，支持日期加减推算。',
      keywords: ['日期计算', '天数计算', '工作日计算', '日期推算', '日历计算', '在线日期'],
      howToTitle: '如何使用日期计算器',
      howTo: '选择开始和结束日期计算差值，或选择起始日期加上天数推算目标日期。工具自动区分工作日和周末。',
      faq: [
        { q: '如何计算工作日？', a: '工具自动排除周六和周日，计算两个日期之间的工作日天数。' },
        { q: '日期计算包含起始日吗？', a: '日期差值计算包含起始日和结束日，即1月1日到1月2日为2天。' },
      ],
    },
    'case-converter': {
      title: '大小写转换',
      description: '在线中英文大小写和繁简体转换工具。支持英文大写、小写、首字母大写，中文简体和繁体互转。',
      keywords: ['大小写转换', '英文大写', '英文小写', '简体转繁体', '繁体转简体', '文本转换'],
      howToTitle: '如何使用大小写转换工具',
      howTo: '输入文本后点击对应的转换按钮：英文大写、英文小写、首字母大写、简体转繁体、繁体转简体、中文数字大小写转换等。',
      faq: [
        { q: '繁体转换准确吗？', a: '工具提供常用繁简字转换，覆盖约500+常用异体字，可满足日常使用需求。' },
        { q: '支持哪些转换类型？', a: '支持英文大小写、首字母大写、简繁体互转、中文数字大小写、反转大小写等8种转换。' },
      ],
    },
    'timezone-converter': {
      title: '时区转换器',
      description: '在线时区时间转换工具。同时显示多个时区的当前时间和日期，轻松对比不同地区的时间差异。',
      keywords: ['时区转换', '世界时间', '时差计算', 'UTC转换', '国际时间', 'TimeZone'],
      howToTitle: '如何使用时区转换器',
      howTo: '选择源时区和目标时区，输入时间即可自动转换。工具同时显示多个主要时区的当前时间，方便快速对比。',
      faq: [
        { q: '时区转换准确吗？', a: '工具基于浏览器时区数据和标准UTC偏移计算，同时考虑了夏令时调整。' },
        { q: '支持哪些时区？', a: '支持所有标准IANA时区，包括北京时间、美东时间、伦敦时间等主流时区。' },
      ],
    },
  },
  about: {
    h1: '关于 ToolZen',
    p1: 'ToolZen 是一组免费在线工具的集合。"Zen" 代表禅意 —— 我们相信好的工具应该简单、干净、不打扰。',
    p2: '与许多同类网站不同，ToolZen 的所有工具都在您的浏览器本地运行。您的数据永远不会上传到服务器，因为我们根本没有后端服务器。',
    p3: '我们的目标是成为开发者、设计师和日常用户的首选在线工具箱。',
    contactTitle: '联系我们',
    contact: '如有任何问题或建议，欢迎联系：',
  },
  privacy: {
    h1: '隐私政策',
    updated: '最后更新：2026-05-16',
    noCollectTitle: '我们不收集您的数据',
    noCollect: 'ToolZen 不会收集、存储或传输您输入到工具中的任何数据。所有处理都在您的浏览器本地完成。',
    analyticsTitle: '数据分析',
    analytics: '我们使用 Google Analytics 4 来统计网站流量。仅收集匿名的页面浏览和大致地理位置（国家级别）数据，不收集任何个人可识别信息。',
    cookiesTitle: 'Cookie',
    cookies: '我们不使用Cookie进行追踪。统计分析使用匿名会话。',
    contactTitle: '联系我们',
    contact: '有疑问？欢迎联系：',
  },
  seo: {
    homeTitle: 'ToolZen - 免费在线工具 | 隐私优先，无需上传',
    homeDesc: 'ToolZen提供JSON格式化、Base64编解码、二维码生成、图片压缩等15+免费在线工具。所有工具在浏览器端运行，数据不上传，隐私安全。',
    aboutTitle: '关于 ToolZen',
    aboutDesc: 'ToolZen是一组免费在线工具的集合，致力于提供隐私优先、简单易用的浏览器端工具。',
    privacyTitle: '隐私政策 - ToolZen',
    privacyDesc: 'ToolZen不会收集、存储或传输您的任何数据。所有处理在浏览器本地完成。',
    categoryPrefix: ' - 免费在线',
  },
  ui: {
    format: '格式化', compress: '压缩', validate: '验证', copy: '复制',
    encode: '编码', decode: '解码', swap: '交换', generate: '生成', download: '下载',
    calculate: '计算', clear: '清除',
    characters: '字符数', charactersNoSpaces: '字符数(不含空格)', words: '单词数',
    lines: '行数', bytes: '字节数', input: '输入', output: '输出',
    typeOrPaste: '输入或粘贴文本...', outputPlaceholder: '输出结果...',
    content: '内容 (URL / 文本)', size: '尺寸', fgColor: '前景色', bgColor: '背景色',
    enterContent: '请输入内容', downloadPng: '下载 PNG', generateFailed: '生成失败',
    loanAmount: '贷款金额 (元)', annualRate: '年利率 (%)', loanYears: '贷款年限',
    repaymentMethod: '还款方式', equalPayment: '等额本息', equalPrincipal: '等额本金',
    monthlyPayment: '月供', totalPayment: '还款总额', totalInterest: '利息总额',
    totalMonths: '还款月数', viewSchedule: '查看还款明细表', period: '期数',
    principal: '本金', interest: '利息', balance: '剩余本金',
    height: '身高 (cm)', weight: '体重 (kg)', yourBmi: '您的 BMI',
    underweight: '偏瘦', normal: '正常', overweight: '偏胖', obese: '肥胖',
    dateDiff: '日期差值', dateAdd: '日期推算', startDate: '开始日期', endDate: '结束日期',
    addDays: '往后推 (天)', totalDays: '总天数', naturalDays: '自然日',
    workdays: '工作日', weekdays: '周一至周五', weekends: '周末', targetDate: '目标日期',
    calcDiff: '计算差值', calcDate: '推算日期',
    weekDayNames: ['日', '一', '二', '三', '四', '五', '六'],
    upperCase: '英文大写', lowerCase: '英文小写', titleCase: '首字母大写',
    s2t: '简体→繁体', t2s: '繁体→简体', numS2F: '中文小写→大写',
    numF2S: '中文大写→小写', invertCase: '反转大小写',
    enterText: '输入文本...', result: '转换结果...',
    selectAlgorithm: '选择算法', generateUuid: '生成', copyAll: '复制全部',
    selectImage: '选择图片', quality: '压缩质量', compressImage: '压缩',
    pattern: '正则表达式', testString: '测试文本',
    originalText: '原始文本', modifiedText: '修改后文本',
    pickColor: '选择颜色',
    sourceTimezone: '源时区', targetTimezone: '目标时区', convertTime: '转换',
  },
  relatedTools: '相关工具',
  toolCount: (n: number) => `${n} 个工具`,
  breadcrumbHome: '首页',
};
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx astro check
```

Expected: pass with no errors related to zh.ts

- [ ] **Step 3: Commit**

```bash
git add src/i18n/zh.ts
git commit -m "feat: add Chinese translation dictionary"
```

---

## Task 3: Create English dictionary (en.ts)

**Files:**
- Create: `src/i18n/en.ts`

- [ ] **Step 1: Write `src/i18n/en.ts` — all English translations**

```typescript
// src/i18n/en.ts
import type { TranslationDict } from './ui';

export const en: TranslationDict = {
  site: {
    name: 'ToolZen',
    tagline: 'Less is More',
  },
  home: {
    hero: 'The Zen of Tools, ',
    heroHighlight: 'Less is More',
    subtitle: '15+ free online tools. Privacy-first, no upload needed. All processing happens in your browser.',
    popular: '⭐ Popular Tools',
    allCategories: '📂 All Categories',
    whyTitle: 'Why ToolZen?',
    privacy: { title: 'Privacy First', desc: 'All tools run locally in your browser. Your data never leaves your device.' },
    speed: { title: 'Blazing Fast', desc: 'Pure static pages with global CDN. Instant results, no waiting.' },
    free: { title: 'Completely Free', desc: 'All tools are free forever. No hidden fees, no feature limits.' },
    faqTitle: 'FAQ',
    faq: [
      { q: 'Are ToolZen tools really free?', a: 'Yes, all tools are free forever. We plan to generate revenue from ads in the future, but will never charge users.' },
      { q: 'Is my data safe?', a: 'Absolutely. All processing happens in your browser. Your data never leaves your device.' },
      { q: 'Do I need to register?', a: 'No. All tools can be used directly without registration.' },
    ],
  },
  nav: {
    dev: 'Developer Tools',
    text: 'Text Tools',
    image: 'Image Tools',
    crypto: 'Crypto & Encoding',
    calculators: 'Calculators',
    searchPlaceholder: 'Search tools... (e.g. JSON, QR, Base64)',
    searchHint: 'Type to search tools',
    searchEmpty: 'No matching tools found',
  },
  footer: {
    copyright: '© 2026 ToolZen. All tools run locally in your browser.',
    about: 'About',
    privacy: 'Privacy',
    contact: 'Contact',
  },
  categories: {
    dev: { title: 'Developer Tools', description: 'JSON, Base64, RegEx and other developer utilities' },
    text: { title: 'Text Tools', description: 'Text diff, word counter and text processing tools' },
    image: { title: 'Image Tools', description: 'Image compression, QR code generator and image utilities' },
    crypto: { title: 'Crypto & Encoding', description: 'MD5, SHA hash, URL encoding and decoding tools' },
    calculators: { title: 'Calculators', description: 'Mortgage, BMI and other practical calculators' },
  },
  tools: {
    'json-formatter': {
      title: 'JSON Formatter',
      description: 'Online JSON formatting, validation, and compression tool. Features syntax highlighting and error detection. All processing happens in your browser.',
      keywords: ['JSON formatter', 'JSON validator', 'online JSON tool', 'JSON beautifier', 'JSON minifier'],
      howToTitle: 'How to Use JSON Formatter',
      howTo: 'Paste your JSON data into the input box and click "Format" to beautify it with proper indentation. You can also compress JSON to a single line, validate JSON syntax, and copy the result with one click. All processing happens locally in your browser — your data is never uploaded to any server.',
      faq: [
        { q: 'What is JSON formatting useful for?', a: 'JSON formatting turns compressed JSON data into an easy-to-read indented format, making it much easier for developers to read and debug. Whether it\'s API responses or configuration files, formatting helps you quickly understand the data structure.' },
        { q: 'Is my data uploaded to a server?', a: 'No. All ToolZen tools run locally in your browser. Your data stays on your device at all times. We never collect, store, or transmit any content you enter.' },
        { q: 'How large a JSON file can I process?', a: 'The JSON formatter runs in your browser and can theoretically handle JSON data up to several megabytes. For very large files, we recommend splitting them first to ensure optimal browser performance. The validation feature helps you confirm JSON syntax is correct before formatting.' },
      ],
    },
    'base64': {
      title: 'Base64 Encoder / Decoder',
      description: 'Online Base64 encoding and decoding tool. Supports text-to-Base64 conversion. Your data never leaves your device.',
      keywords: ['Base64 encode', 'Base64 decode', 'online Base64', 'image to Base64', 'Base64 converter'],
      howToTitle: 'How to Use Base64 Encoder / Decoder',
      howTo: 'Paste text in the input box and click "Encode" for Base64 encoding or "Decode" to decode. Supports proper encoding/decoding of Unicode characters including Chinese. All computation happens in your browser — your data never leaves your device.',
      faq: [
        { q: 'What is Base64 encoding?', a: 'Base64 is a method of converting binary data to ASCII string format, commonly used for transmitting images, files, and other binary content in text-based protocols. It encodes every 3 bytes into 4 printable characters and is widely used in email attachments, Data URIs, and API data transmission.' },
        { q: 'Is my data uploaded to a server?', a: 'No. All ToolZen tools run locally in your browser. Your data stays on your device.' },
        { q: 'How much does Base64 encoding increase data size?', a: 'Base64 encoding increases data size by approximately 33%. This is because every 3 bytes of original data are encoded into 4 bytes. If your application has strict data transfer requirements, use Base64 encoding only when necessary.' },
      ],
    },
    'uuid-generator': {
      title: 'UUID Generator',
      description: 'Online UUID v4 and ULID generator. Supports batch generation and one-click copy. Runs entirely in your browser.',
      keywords: ['UUID generator', 'online UUID', 'GUID generator', 'ULID generator', 'unique identifier'],
      howToTitle: 'How to Use UUID Generator',
      howTo: 'UUID v4 identifiers are generated automatically on page load. Click "Generate" to create multiple UUIDs in batch, with one-click copy for individual or all results. All generation happens client-side with no network requests.',
      faq: [
        { q: 'What is UUID v4?', a: 'UUID v4 is a universally unique identifier generated from random numbers. With an extremely low collision probability, it is widely used for database primary keys, distributed systems, and more.' },
        { q: 'Are the generated UUIDs truly unique?', a: 'UUID v4 uses random number generation with a collision probability of approximately 1/2^122, which is effectively unique for all practical purposes.' },
      ],
    },
    'qr-code': {
      title: 'QR Code Generator',
      description: 'Free online QR code generator. Supports URL, text, contact info and more. Customizable colors and size.',
      keywords: ['QR code generator', 'online QR code', 'free QR code', 'generate QR code', 'QR Code'],
      howToTitle: 'How to Use QR Code Generator',
      howTo: 'Enter a URL or text, adjust size and color settings, then click "Generate" to create your QR code. Supports custom foreground and background colors, and download as PNG image.',
      faq: [
        { q: 'Do generated QR codes expire?', a: 'No. A QR code is simply a graphical encoding of text information. As long as the encoded content is valid, the QR code will work indefinitely.' },
        { q: 'Does it support Chinese characters?', a: 'Yes. Chinese characters are automatically encoded using the appropriate encoding mode.' },
      ],
    },
    'image-compress': {
      title: 'Image Compressor',
      description: 'Online image compression tool. Supports PNG, JPEG, and WebP formats. All compression happens locally, protecting your privacy.',
      keywords: ['image compression', 'compress PNG', 'compress JPEG', 'online image compressor', 'free image compression'],
      howToTitle: 'How to Use Image Compressor',
      howTo: 'Drag and drop or select an image file, adjust the quality slider, and preview the compressed result in real-time. Download the compressed image with one click. All compression happens locally in your browser.',
      faq: [
        { q: 'Will compression affect image quality?', a: 'Moderate compression is barely noticeable to the naked eye. You can preview the result before downloading and adjust quality to your needs.' },
        { q: 'Which image formats are supported?', a: 'PNG, JPEG, and WebP formats are supported for compression.' },
      ],
    },
    'text-diff': {
      title: 'Text Diff Checker',
      description: 'Online text diff comparison tool. Quickly find additions, deletions, and changes between two texts. Supports side-by-side and unified views.',
      keywords: ['text diff', 'diff checker', 'online diff', 'code diff', 'text comparison'],
      howToTitle: 'How to Use Text Diff Checker',
      howTo: 'Paste the original and modified text into the left and right panels. The tool automatically highlights differences — green for additions, red for deletions.',
      faq: [
        { q: 'What formats does text diff support?', a: 'It supports plain text comparison for any format including code, documents, and configuration files.' },
        { q: 'How accurate is the diff result?', a: 'It uses a standard diff algorithm comparing line by line, accurately identifying additions, deletions, and modifications.' },
      ],
    },
    'word-counter': {
      title: 'Word Counter',
      description: 'Online word and character counting tool. Real-time stats for characters, words, lines, and paragraphs. Supports mixed Chinese and English text.',
      keywords: ['word counter', 'character count', 'online word count', 'text statistics'],
      howToTitle: 'How to Use Word Counter',
      howTo: 'Type or paste text into the text area to see real-time statistics including character count, word count, line count, and byte size.',
      faq: [
        { q: 'How are Chinese characters counted?', a: 'Chinese characters are counted individually as characters. For word count, each Chinese character is counted as one character but not included in English word count.' },
        { q: 'Is my data safe?', a: 'All counting happens locally in your browser. Text content is never uploaded to any server.' },
      ],
    },
    'regex-tester': {
      title: 'RegEx Tester',
      description: 'Online regular expression testing tool. Real-time match highlighting with common pattern library. All computation in your browser.',
      keywords: ['regex tester', 'regex online', 'regular expression', 'regex matcher'],
      howToTitle: 'How to Use RegEx Tester',
      howTo: 'Enter a regular expression and test text. The tool highlights all matching results in real-time. Supports global matching, group capturing, and other common features.',
      faq: [
        { q: 'Which regex syntax is supported?', a: 'JavaScript standard regular expression syntax is supported, including groups, quantifiers, and assertions.' },
        { q: 'How does regex handle Chinese characters?', a: 'Unicode property escapes are supported for matching Chinese character ranges.' },
      ],
    },
    'color-picker': {
      title: 'Color Picker',
      description: 'Online color picker and converter. Supports HEX, RGB, and HSL format conversion. Includes palette and eyedropper functionality.',
      keywords: ['color picker', 'eyedropper', 'HEX to RGB', 'online color tool', 'Color Picker'],
      howToTitle: 'How to Use Color Picker',
      howTo: 'Use the color picker to select a color or directly enter HEX/RGB/HSL values. The tool automatically converts and displays values in all supported formats.',
      faq: [
        { q: 'Which color formats are supported?', a: 'HEX, RGB, and HSL formats with mutual conversion are supported.' },
        { q: 'Can I pick colors from my screen?', a: 'Yes, you can use the browser\'s eyedropper tool to pick colors from anywhere on your screen.' },
      ],
    },
    'md5-hash': {
      title: 'MD5 / SHA Hash Generator',
      description: 'Online MD5, SHA-1, and SHA-256 hash generator. Supports text and file hashing. All computation happens in your browser.',
      keywords: ['MD5 hash', 'SHA256 generator', 'hash online', 'file hash', 'MD5 generator'],
      howToTitle: 'How to Use Hash Generator',
      howTo: 'Enter text or select a file, choose the hash algorithm (MD5, SHA-1, SHA-256), and generate the corresponding hash value. All computation happens client-side.',
      faq: [
        { q: 'Is MD5 secure?', a: 'MD5 has known collision vulnerabilities and should not be used for security-sensitive scenarios. Use SHA-256 for security requirements.' },
        { q: 'How is file hashing done?', a: 'After selecting a file, the tool reads the file content in your browser and computes the hash locally. Your file is never uploaded.' },
      ],
    },
    'url-encode': {
      title: 'URL Encoder / Decoder',
      description: 'Online URL encoding and decoding tool. Quickly convert special characters using encodeURI and decodeURI.',
      keywords: ['URL encode', 'URL decode', 'encodeURI', 'decodeURI', 'URL escape'],
      howToTitle: 'How to Use URL Encoder / Decoder',
      howTo: 'Enter text to encode or decode, then click the corresponding button for URL encoding or decoding.',
      faq: [
        { q: 'What is the difference between encoding and decoding?', a: 'URL encoding converts special characters to %XX format for use in URLs. Decoding reverses this by converting %XX back to the original characters.' },
        { q: 'Is my data uploaded?', a: 'No. All conversion happens locally in your browser.' },
      ],
    },
    'mortgage-calculator': {
      title: 'Mortgage Calculator',
      description: 'Online mortgage calculator. Supports equal payment and equal principal methods. Enter loan amount, interest rate, and term to calculate monthly payments and total interest.',
      keywords: ['mortgage calculator', 'monthly payment', 'equal payment', 'equal principal', 'loan calculator'],
      howToTitle: 'How to Use Mortgage Calculator',
      howTo: 'Enter the loan amount, annual interest rate, and loan term. Choose the repayment method (equal payment or equal principal), then click calculate to see monthly payments, total interest, and a detailed amortization schedule.',
      faq: [
        { q: 'What is the difference between equal payment and equal principal?', a: 'Equal payment has fixed monthly payments with higher interest proportion early on. Equal principal has decreasing monthly payments with less total interest but higher initial payments.' },
        { q: 'How accurate are the results?', a: 'Results are based on standard formulas and are for reference only. Actual mortgages may involve additional fees. Please refer to the bank\'s final calculation.' },
      ],
    },
    'bmi-calculator': {
      title: 'BMI Calculator',
      description: 'Online BMI (Body Mass Index) calculator. Enter your height and weight to automatically calculate BMI and health status.',
      keywords: ['BMI calculator', 'body mass index', 'weight index', 'online BMI', 'healthy weight'],
      howToTitle: 'How to Use BMI Calculator',
      howTo: 'Enter your height and weight to automatically calculate your BMI and see your weight status (underweight, normal, overweight, or obese) based on WHO standards.',
      faq: [
        { q: 'Is BMI calculation accurate?', a: 'BMI is a reference indicator and may not apply to athletes, pregnant women, and other special populations. Consult a healthcare professional for health concerns.' },
        { q: 'What is the normal BMI range?', a: 'According to WHO standards, a BMI between 18.5 and 24 is considered normal.' },
      ],
    },
    'date-calculator': {
      title: 'Date Calculator',
      description: 'Online date calculator. Visual calendar selection to calculate days between dates, workdays, and date addition/subtraction.',
      keywords: ['date calculator', 'day counter', 'workday calculator', 'date math', 'online calendar'],
      howToTitle: 'How to Use Date Calculator',
      howTo: 'Select start and end dates to calculate the difference, or choose a start date plus days to calculate a target date. The tool automatically distinguishes workdays and weekends.',
      faq: [
        { q: 'How are workdays calculated?', a: 'The tool automatically excludes Saturdays and Sundays when counting workdays between two dates.' },
        { q: 'Is the start date included?', a: 'Date difference calculation includes both the start and end dates. For example, Jan 1 to Jan 2 counts as 2 days.' },
      ],
    },
    'case-converter': {
      title: 'Case Converter',
      description: 'Online text case and script converter. Supports upper/lowercase, title case, simplified/traditional Chinese, and Chinese numeral conversion.',
      keywords: ['case converter', 'uppercase', 'lowercase', 'simplified to traditional', 'traditional to simplified', 'text converter'],
      howToTitle: 'How to Use Case Converter',
      howTo: 'Enter text and click the desired conversion: UPPERCASE, lowercase, Title Case, Simplified → Traditional Chinese, Traditional → Simplified Chinese, Chinese numeral conversion, and more.',
      faq: [
        { q: 'How accurate is traditional Chinese conversion?', a: 'The tool provides commonly used character conversion covering 500+ variant characters, sufficient for everyday use.' },
        { q: 'What conversion types are supported?', a: '8 conversion types including English case, title case, simplified/traditional Chinese, Chinese numeral formal/simple, and invert case.' },
      ],
    },
    'timezone-converter': {
      title: 'Timezone Converter',
      description: 'Online timezone time converter. Display current time across multiple timezones simultaneously and easily compare time differences.',
      keywords: ['timezone converter', 'world time', 'time difference', 'UTC converter', 'international time'],
      howToTitle: 'How to Use Timezone Converter',
      howTo: 'Select source and target timezones and enter a time to convert. The tool also displays current time across major timezones for quick comparison.',
      faq: [
        { q: 'Is timezone conversion accurate?', a: 'The tool uses browser timezone data and standard UTC offsets, accounting for daylight saving time adjustments.' },
        { q: 'Which timezones are supported?', a: 'All standard IANA timezones are supported, including Beijing Time, Eastern Time, London Time, and other major timezones.' },
      ],
    },
  },
  about: {
    h1: 'About ToolZen',
    p1: 'ToolZen is a collection of free online tools. "Zen" represents simplicity — we believe good tools should be simple, clean, and unobtrusive.',
    p2: 'Unlike many similar sites, all ToolZen tools run locally in your browser. Your data is never uploaded to a server because we have no backend servers at all.',
    p3: 'Our goal is to become the go-to online toolbox for developers, designers, and everyday users.',
    contactTitle: 'Contact',
    contact: 'Have questions or suggestions? Reach us at:',
  },
  privacy: {
    h1: 'Privacy Policy',
    updated: 'Last updated: 2026-05-16',
    noCollectTitle: "We Don't Collect Your Data",
    noCollect: 'ToolZen does not collect, store, or transmit any of the data you enter into our tools. All processing happens locally in your browser.',
    analyticsTitle: 'Analytics',
    analytics: 'We use Google Analytics 4 to measure site traffic. This collects anonymized data such as page views and general location (country level). No personally identifiable information is collected.',
    cookiesTitle: 'Cookies',
    cookies: 'We do not use cookies for tracking. Analytics uses anonymous sessions.',
    contactTitle: 'Contact',
    contact: 'Questions? Contact us at:',
  },
  seo: {
    homeTitle: 'ToolZen - Free Online Tools | Privacy First, No Upload',
    homeDesc: 'ToolZen offers 15+ free online tools including JSON formatter, Base64 encoder/decoder, QR code generator, and image compressor. All tools run in your browser — your data stays private.',
    aboutTitle: 'About ToolZen',
    aboutDesc: 'ToolZen is a collection of free online tools committed to providing privacy-first, easy-to-use browser-based utilities.',
    privacyTitle: 'Privacy Policy - ToolZen',
    privacyDesc: 'ToolZen does not collect, store, or transmit any of your data. All processing happens locally in your browser.',
    categoryPrefix: ' - Free Online ',
  },
  ui: {
    format: 'Format', compress: 'Compress', validate: 'Validate', copy: 'Copy',
    encode: 'Encode', decode: 'Decode', swap: 'Swap', generate: 'Generate', download: 'Download',
    calculate: 'Calculate', clear: 'Clear',
    characters: 'Characters', charactersNoSpaces: 'Chars (no spaces)',
    words: 'Words', lines: 'Lines', bytes: 'Bytes',
    input: 'Input', output: 'Output',
    typeOrPaste: 'Type or paste text here...', outputPlaceholder: 'Output...',
    content: 'Content (URL / Text)', size: 'Size', fgColor: 'Foreground', bgColor: 'Background',
    enterContent: 'Please enter content', downloadPng: 'Download PNG', generateFailed: 'Generation failed',
    loanAmount: 'Loan Amount (¥)', annualRate: 'Annual Rate (%)', loanYears: 'Loan Term (years)',
    repaymentMethod: 'Repayment Method', equalPayment: 'Equal Payment', equalPrincipal: 'Equal Principal',
    monthlyPayment: 'Monthly Payment', totalPayment: 'Total Payment', totalInterest: 'Total Interest',
    totalMonths: 'Total Months', viewSchedule: 'View Amortization Schedule', period: 'Period',
    principal: 'Principal', interest: 'Interest', balance: 'Balance',
    height: 'Height (cm)', weight: 'Weight (kg)', yourBmi: 'Your BMI',
    underweight: 'Underweight', normal: 'Normal', overweight: 'Overweight', obese: 'Obese',
    dateDiff: 'Date Difference', dateAdd: 'Date Add/Subtract', startDate: 'Start Date', endDate: 'End Date',
    addDays: 'Add Days', totalDays: 'Total Days', naturalDays: 'Calendar Days',
    workdays: 'Workdays', weekdays: 'Mon-Fri', weekends: 'Weekends', targetDate: 'Target Date',
    calcDiff: 'Calculate Difference', calcDate: 'Calculate Date',
    weekDayNames: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    upperCase: 'UPPERCASE', lowerCase: 'lowercase', titleCase: 'Title Case',
    s2t: 'Simplified → Traditional', t2s: 'Traditional → Simplified',
    numS2F: 'Chinese Simple → Formal', numF2S: 'Chinese Formal → Simple',
    invertCase: 'Invert Case',
    enterText: 'Enter text...', result: 'Result...',
    selectAlgorithm: 'Select Algorithm', generateUuid: 'Generate', copyAll: 'Copy All',
    selectImage: 'Select Image', quality: 'Quality', compressImage: 'Compress',
    pattern: 'Pattern', testString: 'Test String',
    originalText: 'Original Text', modifiedText: 'Modified Text',
    pickColor: 'Pick Color',
    sourceTimezone: 'Source Timezone', targetTimezone: 'Target Timezone', convertTime: 'Convert',
  },
  relatedTools: 'Related Tools',
  toolCount: (n: number) => `${n} tools`,
  breadcrumbHome: 'Home',
};
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx astro check
```

Expected: pass with no errors

- [ ] **Step 3: Commit**

```bash
git add src/i18n/en.ts
git commit -m "feat: add English translation dictionary"
```

---

## Task 4: Update BaseLayout for bilingual support

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: Update BaseLayout to accept `lang` prop and output hreflang tags**

Read the current file first, then replace its content:

```astro
---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Lang, Translations } from '../i18n';
import { getOtherLang } from '../i18n';

interface Props {
  title: string;
  description: string;
  image?: string;
  lang: Lang;
  t: Translations;
}

const { title, description, image, lang, t } = Astro.props;
const otherLang = getOtherLang(lang);
const currentPath = Astro.url.pathname;
const otherPath = currentPath.replace(`/${lang}/`, `/${otherLang}/`);
---

<!doctype html>
<html lang={lang === 'zh' ? 'zh-CN' : 'en'}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="google-site-verification" content="UOFVIgZElQad5DB9kHrqqSFbjqNrabmSC2E6LGe8Hu8" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="alternate" hreflang={lang} href={new URL(currentPath, Astro.site).href} />
    <link rel="alternate" hreflang={otherLang} href={new URL(otherPath, Astro.site).href} />
    <link rel="alternate" hreflang="x-default" href={new URL(`/en/`, Astro.site).href} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={image || '/og-default.png'} />
    <meta name="twitter:card" content="summary_large_image" />

    <slot name="head" />

    <!-- Google Analytics 4 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W1X2RY2QY7"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-W1X2RY2QY7');
    </script>
  </head>
  <body class="bg-white text-gray-900 antialiased min-h-screen flex flex-col">
    <Header lang={lang} t={t} />
    <main class="flex-1">
      <slot />
    </main>
    <Footer lang={lang} t={t} />
  </body>
</html>

<style is:global>
  @import "tailwindcss";

  @theme {
    --color-zen-50: #eff6ff;
    --color-zen-100: #dbeafe;
    --color-zen-200: #bfdbfe;
    --color-zen-500: #2563eb;
    --color-zen-600: #1d4ed8;
    --color-zen-700: #1e40af;
  }

  * { box-sizing: border-box; }
  body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
  a { color: var(--color-zen-500); }
</style>
```

- [ ] **Step 2: Verify build passes**

```bash
npx astro build
```

Expected: build succeeds (pages still reference old BaseLayout props — expect errors on the page side which get fixed next)

- [ ] **Step 3: Commit**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "feat: update BaseLayout with lang and hreflang support"
```

---

## Task 5: Create root redirect page

**Files:**
- Create: `src/pages/index.astro`
- Move: delete old `src/pages/index.astro` first, or overwrite

- [ ] **Step 1: Write `src/pages/index.astro` — JS-based language redirect**

```astro
---
// src/pages/index.astro
// Root redirect: detects browser language and redirects to /zh/ or /en/
---

<!doctype html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ToolZen</title>
  <script>
    (function() {
      const cookie = document.cookie.split('; ').find(r => r.startsWith('lang='));
      let lang;
      if (cookie) {
        lang = cookie.split('=')[1];
      } else {
        lang = navigator.language.startsWith('zh') ? 'zh' : 'en';
      }
      window.location.replace('/' + lang + '/');
    })();
  </script>
</head>
<body></body>
</html>
```

- [ ] **Step 2: Verify build**

```bash
npx astro build
ls dist/index.html
```

Expected: `dist/index.html` exists with the redirect script

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro
git commit -m "feat: add root language redirect page"
```

---

## Task 6: Create [lang]/index.astro (home page)

**Files:**
- Create: `src/pages/[lang]/index.astro`

- [ ] **Step 1: Create `src/pages/[lang]/` directory and write home page**

```bash
mkdir -p src/pages/\[lang\]/category
```

```astro
---
// src/pages/[lang]/index.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import ToolCard from '../../components/ToolCard.astro';
import CategoryCard from '../../components/CategoryCard.astro';
import SearchBar from '../../components/SearchBar.astro';
import { TOOLS, CATEGORIES } from '../../utils/tools';
import { getLangFromPath, getDict } from '../../i18n';
import type { Lang } from '../../i18n';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const lang = Astro.params.lang as Lang;
const t = getDict(lang);

const popularSlugs = ['json-formatter', 'base64', 'qr-code', 'image-compress', 'uuid-generator', 'word-counter'];
const popularTools = popularSlugs.map(s => TOOLS.find(tool => tool.slug === s)!).filter(Boolean);
---

<BaseLayout lang={lang} t={t} title={t.seo.homeTitle} description={t.seo.homeDesc}>
  <!-- Hero -->
  <section class="max-w-4xl mx-auto px-4 pt-16 pb-12 text-center">
    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4 tracking-tight">
      {t.home.hero}<span class="text-zen-500">{t.home.heroHighlight}</span>
    </h1>
    <p class="text-lg text-gray-500 mb-8 max-w-xl mx-auto">
      {t.home.subtitle}
    </p>
    <SearchBar lang={lang} t={t} />
  </section>

  <!-- Popular Tools -->
  <section class="max-w-6xl mx-auto px-4 py-12">
    <h2 class="text-xl font-semibold text-gray-900 mb-6">{t.home.popular}</h2>
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3">
      {popularTools.map(tool => (
        <ToolCard slug={tool.slug} title={t.tools[tool.slug].title} description={t.tools[tool.slug].description} lang={lang} />
      ))}
    </div>
  </section>

  <!-- All Categories -->
  <section class="max-w-6xl mx-auto px-4 py-12">
    <h2 class="text-xl font-semibold text-gray-900 mb-6">{t.home.allCategories}</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      {CATEGORIES.map(cat => (
        <CategoryCard slug={cat.slug} title={t.categories[cat.slug].title} icon={cat.icon} description={t.categories[cat.slug].description} lang={lang} t={t} />
      ))}
    </div>
  </section>

  <!-- Why ToolZen -->
  <section class="max-w-6xl mx-auto px-4 py-12">
    <h2 class="text-xl font-semibold text-gray-900 mb-6 text-center">{t.home.whyTitle}</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="text-center">
        <div class="text-3xl mb-3">🔒</div>
        <h3 class="font-semibold text-gray-900 mb-2">{t.home.privacy.title}</h3>
        <p class="text-sm text-gray-500">{t.home.privacy.desc}</p>
      </div>
      <div class="text-center">
        <div class="text-3xl mb-3">⚡</div>
        <h3 class="font-semibold text-gray-900 mb-2">{t.home.speed.title}</h3>
        <p class="text-sm text-gray-500">{t.home.speed.desc}</p>
      </div>
      <div class="text-center">
        <div class="text-3xl mb-3">🆓</div>
        <h3 class="font-semibold text-gray-900 mb-2">{t.home.free.title}</h3>
        <p class="text-sm text-gray-500">{t.home.free.desc}</p>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="max-w-4xl mx-auto px-4 py-12 border-t border-gray-100">
    <h2 class="text-xl font-semibold text-gray-900 mb-6">{t.home.faqTitle}</h2>
    <div class="space-y-4">
      {t.home.faq.map(item => (
        <div>
          <h3 class="font-medium text-gray-800">{item.q}</h3>
          <p class="text-gray-600 text-sm mt-1">{item.a}</p>
        </div>
      ))}
    </div>
  </section>
</BaseLayout>
```

- [ ] **Step 2: Verify build**

```bash
npx astro build
ls dist/zh/index.html dist/en/index.html
```

Expected: both files exist

- [ ] **Step 3: Commit**

```bash
git add src/pages/\[lang\]/index.astro
git commit -m "feat: add bilingual home page"
```

---

## Task 7: Create [lang]/about.astro and [lang]/privacy.astro

**Files:**
- Create: `src/pages/[lang]/about.astro`
- Create: `src/pages/[lang]/privacy.astro`

- [ ] **Step 1: Write `src/pages/[lang]/about.astro`**

```astro
---
// src/pages/[lang]/about.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getLangFromPath, getDict } from '../../i18n';
import type { Lang } from '../../i18n';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const lang = Astro.params.lang as Lang;
const t = getDict(lang);
---

<BaseLayout lang={lang} t={t} title={t.seo.aboutTitle} description={t.seo.aboutDesc}>
  <div class="max-w-3xl mx-auto px-4 py-12">
    <h1 class="text-3xl font-bold text-gray-900 mb-6">{t.about.h1}</h1>
    <div class="prose max-w-none text-gray-600 space-y-4">
      <p>{t.about.p1}</p>
      <p>{t.about.p2}</p>
      <p>{t.about.p3}</p>
      <h2 class="text-xl font-semibold text-gray-900 mt-8">{t.about.contactTitle}</h2>
      <p>{t.about.contact} <a href="mailto:hello@toolzen.com">hello@toolzen.com</a></p>
    </div>
  </div>
</BaseLayout>
```

- [ ] **Step 2: Write `src/pages/[lang]/privacy.astro`**

```astro
---
// src/pages/[lang]/privacy.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getLangFromPath, getDict } from '../../i18n';
import type { Lang } from '../../i18n';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const lang = Astro.params.lang as Lang;
const t = getDict(lang);
---

<BaseLayout lang={lang} t={t} title={t.seo.privacyTitle} description={t.seo.privacyDesc}>
  <div class="max-w-3xl mx-auto px-4 py-12">
    <h1 class="text-3xl font-bold text-gray-900 mb-6">{t.privacy.h1}</h1>
    <div class="prose max-w-none text-gray-600 space-y-4">
      <p><strong>{t.privacy.updated}</strong></p>
      <h2 class="text-xl font-semibold text-gray-900">{t.privacy.noCollectTitle}</h2>
      <p>{t.privacy.noCollect}</p>
      <h2 class="text-xl font-semibold text-gray-900">{t.privacy.analyticsTitle}</h2>
      <p>{t.privacy.analytics}</p>
      <h2 class="text-xl font-semibold text-gray-900">{t.privacy.cookiesTitle}</h2>
      <p>{t.privacy.cookies}</p>
      <h2 class="text-xl font-semibold text-gray-900">{t.privacy.contactTitle}</h2>
      <p>{t.privacy.contact} <a href="mailto:hello@toolzen.com">hello@toolzen.com</a></p>
    </div>
  </div>
</BaseLayout>
```

- [ ] **Step 3: Verify build**

```bash
npx astro build
ls dist/zh/about/index.html dist/en/about/index.html dist/zh/privacy/index.html dist/en/privacy/index.html
```

Expected: all four files exist

- [ ] **Step 4: Commit**

```bash
git add src/pages/\[lang\]/about.astro src/pages/\[lang\]/privacy.astro
git commit -m "feat: add bilingual about and privacy pages"
```

---

## Task 8: Create [lang]/category/[cat].astro (category catch-all)

**Files:**
- Create: `src/pages/[lang]/category/[cat].astro`

- [ ] **Step 1: Write category catch-all page**

```astro
---
// src/pages/[lang]/category/[cat].astro
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Breadcrumb from '../../../components/Breadcrumb.astro';
import ToolCard from '../../../components/ToolCard.astro';
import { getToolsByCategory, CATEGORIES } from '../../../utils/tools';
import { getLangFromPath, getDict } from '../../../i18n';
import type { Lang } from '../../../i18n';

export function getStaticPaths() {
  const paths: Array<{ params: { lang: string; cat: string } }> = [];
  for (const lang of ['zh', 'en']) {
    for (const cat of CATEGORIES) {
      paths.push({ params: { lang, cat: cat.slug } });
    }
  }
  return paths;
}

const { lang: langParam, cat } = Astro.params as { lang: string; cat: string };
const lang: Lang = langParam === 'en' ? 'en' : 'zh';
const t = getDict(lang);
const catData = CATEGORIES.find(c => c.slug === cat)!;
const tools = getToolsByCategory(cat);
const catMeta = t.categories[cat];
const title = `${catMeta.title}${t.seo.categoryPrefix}${catMeta.title}`;
---

<BaseLayout lang={lang} t={t} title={title} description={catMeta.description}>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <Breadcrumb items={[
      { label: t.breadcrumbHome, href: `/${lang}/` },
      { label: catMeta.title },
    ]} lang={lang} />

    <h1 class="text-3xl font-bold text-gray-900 mb-2">{catData.icon} {catMeta.title}</h1>
    <p class="text-gray-500 mb-8">{catMeta.description}</p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      {tools.map(tool => (
        <ToolCard slug={tool.slug} title={t.tools[tool.slug].title} description={t.tools[tool.slug].description} lang={lang} />
      ))}
    </div>
  </div>
</BaseLayout>
```

- [ ] **Step 2: Verify build**

```bash
npx astro build
ls dist/zh/category/dev/index.html dist/en/category/dev/index.html
```

Expected: both category directories exist

- [ ] **Step 3: Commit**

```bash
git add src/pages/\[lang\]/category/\[cat\].astro
git commit -m "feat: add bilingual category pages"
```

---

## Task 9: Create [lang] tool pages for all 15 tools

**Files:**
- Create: `src/pages/[lang]/json-formatter.astro` (and 14 more)

Each tool page follows the same pattern. Here's `json-formatter.astro` — repeat for all 15:

- [ ] **Step 1: Write `src/pages/[lang]/json-formatter.astro`**

```astro
---
// src/pages/[lang]/json-formatter.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import RelatedTools from '../../components/RelatedTools.astro';
import { getTool } from '../../utils/tools';
import { getLangFromPath, getDict } from '../../i18n';
import type { Lang } from '../../i18n';
import JsonFormatter from '../../islands/JsonFormatter';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const slug = 'json-formatter';
const lang = Astro.params.lang as Lang;
const t = getDict(lang);
const tool = getTool(slug)!;
const toolMeta = t.tools[slug];
const catMeta = t.categories[tool.category];
const title = `${toolMeta.title} | ToolZen`;
---

<BaseLayout lang={lang} t={t} title={title} description={toolMeta.description}>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <Breadcrumb items={[
      { label: t.breadcrumbHome, href: `/${lang}/` },
      { label: catMeta.title, href: `/${lang}/category/${tool.category}/` },
      { label: toolMeta.title },
    ]} lang={lang} />

    <h1 class="text-3xl font-bold text-gray-900 mb-2">{toolMeta.title}</h1>
    <p class="text-gray-500 mb-8">{toolMeta.description}</p>

    <JsonFormatter client:load />

    <section class="mt-12 prose max-w-none">
      <h2 class="text-xl font-semibold text-gray-900">{toolMeta.howToTitle}</h2>
      <p class="text-gray-600">{toolMeta.howTo}</p>

      <h3 class="text-lg font-semibold text-gray-900 mt-6">FAQ</h3>
      <div class="space-y-4">
        {toolMeta.faq.map(item => (
          <div>
            <h4 class="font-medium text-gray-800">{item.q}</h4>
            <p class="text-gray-600">{item.a}</p>
          </div>
        ))}
      </div>
    </section>

    <RelatedTools current={slug} lang={lang} t={t} />
  </div>

  <slot name="head">
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'WebApplication',
      name: toolMeta.title,
      description: toolMeta.description,
      url: `${Astro.site}${lang}/${slug}/`,
      applicationCategory: 'UtilityApplication',
      operatingSystem: 'All',
      offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    })}></script>
  </slot>
</BaseLayout>
```

- [ ] **Step 2: Write the remaining 14 tool pages**

Each follows the identical pattern above, only differing in:
- `slug` constant value
- The island component import

Create these files: `base64.astro`, `uuid-generator.astro`, `qr-code.astro`, `image-compress.astro`, `text-diff.astro`, `word-counter.astro`, `regex-tester.astro`, `color-picker.astro`, `md5-hash.astro`, `url-encode.astro`, `mortgage-calculator.astro`, `bmi-calculator.astro`, `date-calculator.astro`, `case-converter.astro`, `timezone-converter.astro`

For each, copy the json-formatter pattern and change: `slug`, the island import, and the island tag name.

Example for `base64.astro`:
```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import RelatedTools from '../../components/RelatedTools.astro';
import { getTool } from '../../utils/tools';
import { getLangFromPath, getDict } from '../../i18n';
import type { Lang } from '../../i18n';
import Base64Tool from '../../islands/Base64Tool';

export function getStaticPaths() {
  return [{ params: { lang: 'zh' } }, { params: { lang: 'en' } }];
}

const slug = 'base64';
const lang = Astro.params.lang as Lang;
const t = getDict(lang);
const tool = getTool(slug)!;
const toolMeta = t.tools[slug];
const catMeta = t.categories[tool.category];
const title = `${toolMeta.title} | ToolZen`;
---

<BaseLayout lang={lang} t={t} title={title} description={toolMeta.description}>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <Breadcrumb items={[
      { label: t.breadcrumbHome, href: `/${lang}/` },
      { label: catMeta.title, href: `/${lang}/category/${tool.category}/` },
      { label: toolMeta.title },
    ]} lang={lang} />

    <h1 class="text-3xl font-bold text-gray-900 mb-2">{toolMeta.title}</h1>
    <p class="text-gray-500 mb-8">{toolMeta.description}</p>

    <Base64Tool client:load />

    <section class="mt-12 prose max-w-none">
      <h2 class="text-xl font-semibold text-gray-900">{toolMeta.howToTitle}</h2>
      <p class="text-gray-600">{toolMeta.howTo}</p>

      <h3 class="text-lg font-semibold text-gray-900 mt-6">FAQ</h3>
      <div class="space-y-4">
        {toolMeta.faq.map(item => (
          <div>
            <h4 class="font-medium text-gray-800">{item.q}</h4>
            <p class="text-gray-600">{item.a}</p>
          </div>
        ))}
      </div>
    </section>

    <RelatedTools current={slug} lang={lang} t={t} />
  </div>

  <slot name="head">
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'WebApplication',
      name: toolMeta.title,
      description: toolMeta.description,
      url: `${Astro.site}${lang}/${slug}/`,
      applicationCategory: 'UtilityApplication',
      operatingSystem: 'All',
      offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    })}></script>
  </slot>
</BaseLayout>
```

Repeat for all 15 slugs. Island imports per slug:

| Slug | Island Import |
|------|--------------|
| json-formatter | JsonFormatter |
| base64 | Base64Tool |
| uuid-generator | UuidGenerator |
| qr-code | QrCodeGenerator |
| image-compress | ImageCompress |
| text-diff | TextDiff |
| word-counter | WordCounter |
| regex-tester | RegexTester |
| color-picker | ColorPicker |
| md5-hash | HashGenerator |
| url-encode | UrlEncoder |
| mortgage-calculator | MortgageCalculator |
| bmi-calculator | BmiCalculator |
| date-calculator | DateCalculator |
| case-converter | CaseConverter |
| timezone-converter | TimezoneConverter |

- [ ] **Step 3: Verify build**

```bash
npx astro build
ls dist/zh/json-formatter/index.html dist/en/json-formatter/index.html
ls dist/zh/base64/index.html dist/en/base64/index.html
```

Expected: all 30 tool pages (15 zh + 15 en) exist

- [ ] **Step 4: Commit**

```bash
git add src/pages/\[lang\]/*.astro
git commit -m "feat: add bilingual tool pages for all 15 tools"
```

---

## Task 10: Update Astro components for translations

**Files:**
- Modify: `src/components/Header.astro`
- Modify: `src/components/Footer.astro`
- Modify: `src/components/Breadcrumb.astro`
- Modify: `src/components/ToolCard.astro`
- Modify: `src/components/CategoryCard.astro`
- Modify: `src/components/RelatedTools.astro`
- Modify: `src/components/SearchBar.astro`

- [ ] **Step 1: Update `src/components/Header.astro`**

```astro
---
import SearchModal from '../islands/SearchModal';
import type { Lang, Translations } from '../i18n';
import { switchPath } from '../i18n';

interface Props {
  lang: Lang;
  t: Translations;
}

const { lang, t } = Astro.props;
const navItems = [
  { label: t.nav.dev, href: `/${lang}/category/dev/` },
  { label: t.nav.text, href: `/${lang}/category/text/` },
  { label: t.nav.image, href: `/${lang}/category/image/` },
  { label: t.nav.crypto, href: `/${lang}/category/crypto/` },
  { label: t.nav.calculators, href: `/${lang}/category/calculators/` },
];
const otherPath = switchPath(Astro.url.pathname);
const otherLabel = lang === 'zh' ? 'EN' : '中文';
---

<header class="sticky top-0 z-50 bg-white/80 backdrop-blur border-b border-gray-100">
  <div class="max-w-6xl mx-auto px-4 h-14 flex items-center justify-between">
    <a href={`/${lang}/`} class="flex items-center gap-2 font-bold text-xl text-gray-900 no-underline">
      <span class="w-8 h-8 bg-zen-500 rounded-lg flex items-center justify-center text-white text-sm">T</span>
      ToolZen
    </a>

    <nav class="hidden md:flex items-center gap-1">
      {navItems.map(item => (
        <a href={item.href} class="px-3 py-2 text-sm text-gray-600 hover:text-zen-500 rounded-md hover:bg-zen-50 transition-colors no-underline">
          {item.label}
        </a>
      ))}
    </nav>

    <div class="flex items-center gap-2">
      <a href={otherPath} onclick={`document.cookie='lang=${lang === 'zh' ? 'en' : 'zh'};max-age=31536000;path=/';`}
        class="px-2 py-1 text-xs text-gray-500 hover:text-zen-500 border border-gray-200 rounded-md no-underline transition-colors">
        {otherLabel}
      </a>
      <SearchModal client:load />
    </div>
  </div>
</header>
```

- [ ] **Step 2: Update `src/components/Footer.astro`**

```astro
---
import type { Lang, Translations } from '../i18n';

interface Props {
  lang: Lang;
  t: Translations;
}

const { lang, t } = Astro.props;
---

<footer class="border-t border-gray-100 mt-20">
  <div class="max-w-6xl mx-auto px-4 py-10">
    <div class="flex flex-col md:flex-row justify-between items-center gap-4">
      <div class="text-sm text-gray-500">
        {t.footer.copyright}
      </div>
      <nav class="flex gap-6">
        <a href={`/${lang}/about/`} class="text-sm text-gray-500 hover:text-gray-700 no-underline">{t.footer.about}</a>
        <a href={`/${lang}/privacy/`} class="text-sm text-gray-500 hover:text-gray-700 no-underline">{t.footer.privacy}</a>
        <a href="mailto:hello@toolzen.com" class="text-sm text-gray-500 hover:text-gray-700 no-underline">{t.footer.contact}</a>
      </nav>
    </div>
  </div>
</footer>
```

- [ ] **Step 3: Update `src/components/Breadcrumb.astro`**

```astro
---
export interface Props {
  items: Array<{ label: string; href?: string }>;
  lang: string;
}

const { items, lang } = Astro.props;
const breadcrumbLD = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: items.map((item, i) => ({
    '@type': 'ListItem',
    position: i + 1,
    name: item.label,
    item: item.href ? `https://toolzen.com${item.href}` : undefined,
  })),
});
---

<nav class="flex items-center gap-2 text-sm text-gray-400 mb-6" aria-label="Breadcrumb">
  {items.map((item, i) => (
    <>
      {i > 0 && <span>/</span>}
      {item.href ? (
        <a href={item.href} class="hover:text-gray-600 transition-colors no-underline">{item.label}</a>
      ) : (
        <span class="text-gray-700">{item.label}</span>
      )}
    </>
  ))}
</nav>

<script type="application/ld+json" set:html={breadcrumbLD}></script>
```

- [ ] **Step 4: Update `src/components/ToolCard.astro`**

```astro
---
export interface Props {
  slug: string;
  title: string;
  description: string;
  lang: string;
}
const { slug, title, description, lang } = Astro.props;
---

<a href={`/${lang}/${slug}/`} class="block p-4 rounded-xl border border-gray-100 hover:border-zen-200 hover:bg-zen-50/30 transition-all no-underline group" data-tool-card={title}>
  <h3 class="font-semibold text-gray-900 group-hover:text-zen-500 transition-colors">{title}</h3>
  <p class="text-sm text-gray-500 mt-1">{description.slice(0, 80)}...</p>
</a>
```

- [ ] **Step 5: Update `src/components/CategoryCard.astro`**

```astro
---
import { getToolsByCategory } from '../utils/tools';
import type { Translations } from '../i18n';

export interface Props {
  slug: string;
  title: string;
  icon: string;
  description: string;
  lang: string;
  t: Translations;
}
const { slug, title, icon, description, lang, t } = Astro.props;
const count = getToolsByCategory(slug).length;
---

<a href={`/${lang}/category/${slug}/`} class="block p-5 rounded-xl border border-gray-100 hover:border-zen-200 hover:bg-zen-50/30 transition-all no-underline group">
  <div class="text-2xl mb-2">{icon}</div>
  <h3 class="font-semibold text-gray-900 group-hover:text-zen-500 transition-colors">{title}</h3>
  <p class="text-sm text-gray-500 mt-1">{description}</p>
  <span class="inline-block mt-2 text-xs text-zen-500 bg-zen-50 px-2 py-0.5 rounded-full">{t.toolCount(count)}</span>
</a>
```

- [ ] **Step 6: Update `src/components/RelatedTools.astro`**

```astro
---
import { getRelatedTools } from '../utils/tools';
import ToolCard from './ToolCard.astro';
import type { Translations } from '../i18n';

export interface Props {
  current: string;
  lang: string;
  t: Translations;
}
const { current, lang, t } = Astro.props;
const related = getRelatedTools(current, 4);
---

{related.length > 0 && (
  <section class="mt-12 pt-8 border-t border-gray-100">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">{t.relatedTools}</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
      {related.map(tool => (
        <ToolCard slug={tool.slug} title={t.tools[tool.slug].title} description={t.tools[tool.slug].description} lang={lang} />
      ))}
    </div>
  </section>
)}
```

- [ ] **Step 7: Update `src/components/SearchBar.astro`**

```astro
---
import type { Translations } from '../i18n';

interface Props {
  lang: string;
  t: Translations;
}

const { lang, t } = Astro.props;
---

<div id="search" class="relative max-w-2xl mx-auto">
  <input
    type="text"
    id="tool-search"
    placeholder={t.nav.searchPlaceholder}
    class="w-full px-4 py-3 pl-10 rounded-xl border border-gray-200 focus:border-zen-500 focus:ring-2 focus:ring-zen-100 outline-none transition-all text-gray-900"
  />
  <svg class="absolute left-3 top-3.5 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
  </svg>
</div>

<script>
  const input = document.getElementById('tool-search') as HTMLInputElement;
  input?.addEventListener('input', () => {
    const q = input.value.toLowerCase();
    const cards = document.querySelectorAll('[data-tool-card]');
    cards.forEach(card => {
      const name = (card.getAttribute('data-tool-card') || '').toLowerCase();
      (card as HTMLElement).style.display = name.includes(q) ? '' : 'none';
    });
  });
</script>
```

- [ ] **Step 8: Verify build**

```bash
npx astro build
```

Expected: build succeeds

- [ ] **Step 9: Commit**

```bash
git add src/components/
git commit -m "feat: add translation support to all Astro components and language switcher"
```

---

## Task 11: Create useLanguage hook for Preact islands

**Files:**
- Create: `src/hooks/useLanguage.ts`
- Modify: All 15 island `.tsx` files

- [ ] **Step 1: Write `src/hooks/useLanguage.ts`**

```typescript
// src/hooks/useLanguage.ts
import { useState, useEffect } from 'preact/hooks';
import type { Translations, Lang } from '../i18n';
import { getDict } from '../i18n';

export function useLanguage(): { t: Translations; lang: Lang } {
  const [lang, setLang] = useState<Lang>('en');

  useEffect(() => {
    const htmlLang = document.documentElement.lang;
    if (htmlLang.startsWith('zh')) {
      setLang('zh');
    } else {
      setLang('en');
    }
  }, []);

  return { t: getDict(lang), lang };
}
```

- [ ] **Step 2: Update islands to use `useLanguage()` hook**

Each island currently has hardcoded Chinese strings. Update them to use `useLanguage()`.

Example: `src/islands/JsonFormatter.tsx`:

```tsx
import { useState } from 'preact/hooks';
import { useLanguage } from '../hooks/useLanguage';

export default function JsonFormatter() {
  const [input, setInput] = useState('');
  const [output, setOutput] = useState('');
  const [error, setError] = useState('');
  const { t } = useLanguage();

  const format = () => {
    try {
      const obj = JSON.parse(input);
      setOutput(JSON.stringify(obj, null, 2));
      setError('');
    } catch (e) {
      setError((e as Error).message);
      setOutput('');
    }
  };

  const compress = () => {
    try {
      const obj = JSON.parse(input);
      setOutput(JSON.stringify(obj));
      setError('');
    } catch (e) {
      setError((e as Error).message);
      setOutput('');
    }
  };

  const validate = () => {
    try {
      JSON.parse(input);
      setError('');
      setOutput('✓ Valid JSON');
    } catch (e) {
      setError((e as Error).message);
      setOutput('');
    }
  };

  const copy = () => { navigator.clipboard.writeText(output); };

  return (
    <div class="space-y-4">
      <div class="flex gap-2 flex-wrap">
        <button onClick={format} class="px-4 py-2 bg-zen-500 text-white rounded-lg text-sm font-medium hover:bg-zen-600 transition-colors">Format</button>
        <button onClick={compress} class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg text-sm font-medium hover:bg-gray-200 transition-colors">Compress</button>
        <button onClick={validate} class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg text-sm font-medium hover:bg-gray-200 transition-colors">Validate</button>
        <button onClick={copy} class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg text-sm font-medium hover:bg-gray-200 transition-colors">Copy</button>
      </div>
      <textarea value={input} onInput={(e) => setInput((e.target as HTMLTextAreaElement).value)}
        placeholder='{"key": "value"}'
        rows={10}
        class="w-full p-3 border border-gray-200 rounded-lg font-mono text-sm focus:border-zen-500 focus:ring-1 focus:ring-zen-100 outline-none resize-y" />
      {error && <div class="p-3 bg-red-50 border border-red-100 rounded-lg text-sm text-red-600 font-mono">{error}</div>}
      <textarea value={output} readOnly rows={10}
        class="w-full p-3 bg-gray-50 border border-gray-200 rounded-lg font-mono text-sm resize-y"
        placeholder="Output..." />
    </div>
  );
}
```

Note: For islands with hardcoded Chinese labels, add entries to the translation dictionary under the `tools.<slug>` namespace and use `t.tools.<slug>` in the component.

Islands that need translation updates (Chinese → dictionary):
- `JsonFormatter.tsx` — button labels are already English, no change needed
- `Base64Tool.tsx` — labels already English, "Invalid Base64 input" string needs i18n
- `UuidGenerator.tsx` — button labels need translation
- `UrlEncoder.tsx` — labels already English
- `WordCounter.tsx` — stat labels (Characters, Words, Lines, Bytes) + placeholder
- `RegexTester.tsx` — labels + placeholders
- `HashGenerator.tsx` — labels + placeholders
- `TextDiff.tsx` — labels + placeholders
- `ImageCompress.tsx` — labels + placeholders
- `ColorPicker.tsx` — labels
- `QrCodeGenerator.tsx` — labels (内容, 尺寸, 生成, 下载PNG, 请输入内容, 生成失败)
- `MortgageCalculator.tsx` — labels (贷款金额, 年利率, etc.) + table headers
- `BmiCalculator.tsx` — labels (身高, 体重, etc.) + status labels (偏瘦, 正常, etc.)
- `DateCalculator.tsx` — labels (日期差值, 日期推算, 开始日期, etc.) + weekday names
- `CaseConverter.tsx` — button labels (英文大写, 英文小写, etc.)

The `ui` namespace and `UIDict` type are already defined in Tasks 1-3. Islands reference `t.ui` for their labels.

- [ ] **Step 3: Verify build**

```bash
npx astro build
```

Expected: build succeeds without errors

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useLanguage.ts src/islands/
git commit -m "feat: add useLanguage hook and update islands for i18n"
```

---

## Task 12: Update sitemap for bilingual URLs

**Files:**
- Modify: `src/pages/sitemap-index.xml.ts`

- [ ] **Step 1: Update sitemap to list all zh + en URLs**

```typescript
// src/pages/sitemap-index.xml.ts
import { TOOLS, CATEGORIES } from '../utils/tools';

export async function GET() {
  const base = 'https://toolzen.com';
  const langs = ['zh', 'en'];

  const urls: Array<{ loc: string; priority: string }> = [];

  for (const lang of langs) {
    urls.push({ loc: `${base}/${lang}/`, priority: '1.0' });
    urls.push({ loc: `${base}/${lang}/about/`, priority: '0.5' });
    urls.push({ loc: `${base}/${lang}/privacy/`, priority: '0.5' });
    for (const tool of TOOLS) {
      urls.push({ loc: `${base}/${lang}/${tool.slug}/`, priority: '0.9' });
    }
    for (const cat of CATEGORIES) {
      urls.push({ loc: `${base}/${lang}/category/${cat.slug}/`, priority: '0.7' });
    }
  }

  const xml = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  ${urls.map(u => `  <url><loc>${u.loc}</loc><priority>${u.priority}</priority></url>`).join('\n')}
</urlset>`;

  return new Response(xml, {
    headers: { 'Content-Type': 'application/xml' },
  });
}
```

- [ ] **Step 2: Verify build produces sitemap**

```bash
npx astro build
cat dist/sitemap-index.xml | head -20
```

Expected: XML with both zh and en URLs

- [ ] **Step 3: Commit**

```bash
git add src/pages/sitemap-index.xml.ts
git commit -m "feat: update sitemap with bilingual URLs"
```

---

## Task 13: Remove old page files and final verification

**Files:**
- Delete old pages that have been migrated to `[lang]/`

- [ ] **Step 1: Delete old page files**

```bash
rm -f src/pages/about.astro
rm -f src/pages/privacy.astro
rm -f src/pages/json-formatter.astro
rm -f src/pages/base64.astro
rm -f src/pages/uuid-generator.astro
rm -f src/pages/qr-code.astro
rm -f src/pages/image-compress.astro
rm -f src/pages/text-diff.astro
rm -f src/pages/word-counter.astro
rm -f src/pages/regex-tester.astro
rm -f src/pages/color-picker.astro
rm -f src/pages/md5-hash.astro
rm -f src/pages/url-encode.astro
rm -f src/pages/mortgage-calculator.astro
rm -f src/pages/bmi-calculator.astro
rm -f src/pages/date-calculator.astro
rm -f src/pages/case-converter.astro
rm -f src/pages/timezone-converter.astro
rm -f src/pages/category/dev.astro
rm -f src/pages/category/text.astro
rm -f src/pages/category/image.astro
rm -f src/pages/category/crypto.astro
rm -f src/pages/category/calculators.astro
rmdir src/pages/category 2>/dev/null || true
```

- [ ] **Step 2: Run full build**

```bash
npx astro build
```

Expected: clean build with no errors

- [ ] **Step 3: Check build output structure**

```bash
echo "=== dist/ structure ==="
ls dist/
echo "=== zh pages ==="
ls dist/zh/
echo "=== en pages ==="
ls dist/en/
echo "=== zh category ==="
ls dist/zh/category/
echo "=== sitemap check ==="
wc -l dist/sitemap-index.xml
```

Expected: `dist/zh/` and `dist/en/` each contain all pages, sitemap has 40+ entries

- [ ] **Step 4: Verify SEO tags in output**

```bash
head -20 dist/zh/index.html
head -20 dist/en/json-formatter/index.html
```

Expected: hreflang links present, correct lang attribute on `<html>`, translated title/description

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: remove old pages, finalize i18n migration"
```

---

## Summary

| Task | Description | Files |
|------|-------------|-------|
| 1 | Type system + translator | Create `src/i18n/ui.ts`, `src/i18n/index.ts` |
| 2 | Chinese dictionary | Create `src/i18n/zh.ts` |
| 3 | English dictionary | Create `src/i18n/en.ts` |
| 4 | BaseLayout update | Modify `src/layouts/BaseLayout.astro` |
| 5 | Root redirect | Create `src/pages/index.astro` |
| 6 | Home page | Create `src/pages/[lang]/index.astro` |
| 7 | About + Privacy | Create `src/pages/[lang]/about.astro`, `privacy.astro` |
| 8 | Category pages | Create `src/pages/[lang]/category/[cat].astro` |
| 9 | Tool pages (15) | Create 15 files under `src/pages/[lang]/` |
| 10 | Components update | Modify 7 files under `src/components/` |
| 11 | Islands + hook | Create `src/hooks/useLanguage.ts`, modify 15 islands |
| 12 | Sitemap | Modify `src/pages/sitemap-index.xml.ts` |
| 13 | Cleanup + verify | Delete old files, final build check |
